# Socket.IO URL (optional; Vite dev defaults to VITE_REACT_APP_API_BASE_URL / :3000)
# Leave empty in Docker — nginx proxies /socket.io on the same origin.
# VITE_REACT_APP_WEBSOCKET_URL=http://localhost:3000

# API Configuration
VITE_REACT_APP_API_BASE_URL=http://localhost:3000

# Preview ClinicalNoteViewer on home without recording (vite dev, or VITE_DEV=true)
# VITE_DEV_PREVIEW_NOTE_ID=your-clinical-note-uuid
